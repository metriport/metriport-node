/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * A summary of information based on the results of executing a TestScript.
 */
export interface TestReport extends Metriport.fhir.BaseResource {
    resourceType: "TestReport";
    /** Identifier for the TestScript assigned for external purposes outside the context of FHIR. */
    identifier?: Metriport.fhir.Identifier;
    /** A free text natural language name identifying the executed TestScript. */
    name?: string;
    /** The current state of this test report. */
    status?: Metriport.fhir.TestReportStatus;
    /** Ideally this is an absolute URL that is used to identify the version-specific TestScript that was executed, matching the `TestScript.url`. */
    testScript: Metriport.fhir.Reference;
    /** The overall result from the execution of the TestScript. */
    result?: Metriport.fhir.TestReportResult;
    /** The final score (percentage of tests passed) resulting from the execution of the TestScript. */
    score?: Metriport.fhir.Decimal;
    /** Name of the tester producing this report (Organization or individual). */
    tester?: string;
    /** When the TestScript was executed and this TestReport was generated. */
    issued?: Metriport.fhir.DateTime;
    /** A participant in the test execution, either the execution engine, a client, or a server. */
    participant?: Metriport.fhir.TestReportParticipant[];
    /** The results of the series of required setup operations before the tests were executed. */
    setup?: Metriport.fhir.TestReportSetup;
    /** A test executed from the test script. */
    test?: Metriport.fhir.TestReportTest[];
    /** The results of the series of operations required to clean up after all the tests were executed (successfully or otherwise). */
    teardown?: Metriport.fhir.TestReportTeardown;
}
