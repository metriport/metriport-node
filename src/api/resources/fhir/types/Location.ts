/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * Details and position information for a physical place where services are provided and resources and participants may be stored, found, contained, or accommodated.
 */
export interface Location extends Metriport.fhir.BaseResource {
    resourceType: "Location";
    /** Unique code or number identifying the location to its users. */
    identifier?: Metriport.fhir.Identifier[];
    /** The status property covers the general availability of the resource, not the current value which may be covered by the operationStatus, or by a schedule/slots if they are configured for the location. */
    status?: Metriport.fhir.LocationStatus;
    /** The operational status covers operation values most relevant to beds (but can also apply to rooms/units/chairs/etc. such as an isolation unit/dialysis chair). This typically covers concepts such as contamination, housekeeping, and other activities like maintenance. */
    operationalStatus?: Metriport.fhir.Coding;
    /** Name of the location as used by humans. Does not need to be unique. */
    name?: string;
    /** A list of alternate names that the location is known as, or was known as, in the past. */
    alias?: string[];
    /** Description of the Location, which helps in finding or referencing the place. */
    description?: string;
    /** Indicates whether a resource instance represents a specific location or a class of locations. */
    mode?: Metriport.fhir.LocationMode;
    /** Indicates the type of function performed at the location. */
    type?: Metriport.fhir.CodeableConcept[];
    /** The contact details of communication devices available at the location. This can include phone numbers, fax numbers, mobile numbers, email addresses and web sites. */
    telecom?: Metriport.fhir.ContactPoint[];
    /** Physical location. */
    address?: Metriport.fhir.Address;
    /** Physical form of the location, e.g. building, room, vehicle, road. */
    physicalType?: Metriport.fhir.CodeableConcept;
    /** The absolute geographic location of the Location, expressed using the WGS84 datum (This is the same co-ordinate system used in KML). */
    position?: Metriport.fhir.LocationPosition;
    /** The organization responsible for the provisioning and upkeep of the location. */
    managingOrganization?: Metriport.fhir.Reference;
    /** Another Location of which this Location is physically a part of. */
    partOf?: Metriport.fhir.Reference;
    /** What days/times during a week is this location usually open. */
    hoursOfOperation?: Metriport.fhir.LocationHoursOfOperation[];
    /** A description of when the locations opening ours are different to normal, e.g. public holiday availability. Succinctly describing all possible exceptions to normal site availability as detailed in the opening hours Times. */
    availabilityExceptions?: string;
    /** Technical endpoints providing access to services operated for the location. */
    endpoint?: Metriport.fhir.Reference[];
}
