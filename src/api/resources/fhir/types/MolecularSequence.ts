/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * Raw data describing a biological sequence.
 */
export interface MolecularSequence extends Metriport.fhir.BaseResource {
    resourceType: "MolecularSequence";
    /** A unique identifier for this particular sequence instance. This is a FHIR-defined id. */
    identifier?: Metriport.fhir.Identifier[];
    /** Amino Acid Sequence/ DNA Sequence / RNA Sequence. */
    type?: Metriport.fhir.MolecularSequenceType;
    /** Whether the sequence is numbered starting at 0 (0-based numbering or coordinates, inclusive start, exclusive end) or starting at 1 (1-based numbering, inclusive start and inclusive end). */
    coordinateSystem?: number;
    /** The patient whose sequencing results are described by this resource. */
    patient?: Metriport.fhir.Reference;
    /** Specimen used for sequencing. */
    specimen?: Metriport.fhir.Reference;
    /** The method for sequencing, for example, chip information. */
    device?: Metriport.fhir.Reference;
    /** The organization or lab that should be responsible for this result. */
    performer?: Metriport.fhir.Reference;
    /** The number of copies of the sequence of interest. (RNASeq). */
    quantity?: Metriport.fhir.Quantity;
    /** A sequence that is used as a reference to describe variants that are present in a sequence analyzed. */
    referenceSeq?: Metriport.fhir.MolecularSequenceReferenceSeq;
    /** The definition of variant here originates from Sequence ontology ([variant_of](http://www.sequenceontology.org/browser/current_svn/term/variant_of)). This element can represent amino acid or nucleic sequence change(including insertion,deletion,SNP,etc.) It can represent some complex mutation or segment variation with the assist of CIGAR string. */
    variant?: Metriport.fhir.MolecularSequenceVariant[];
    /** Sequence that was observed. It is the result marked by referenceSeq along with variant records on referenceSeq. This shall start from referenceSeq.windowStart and end by referenceSeq.windowEnd. */
    observedSeq?: string;
    /** An experimental feature attribute that defines the quality of the feature in a quantitative way, such as a phred quality score ([SO:0001686](http://www.sequenceontology.org/browser/current_svn/term/SO:0001686)). */
    quality?: Metriport.fhir.MolecularSequenceQuality[];
    /** Coverage (read depth or depth) is the average number of reads representing a given nucleotide in the reconstructed sequence. */
    readCoverage?: number;
    /** Configurations of the external repository. The repository shall store target's observedSeq or records related with target's observedSeq. */
    repository?: Metriport.fhir.MolecularSequenceRepository[];
    /** Pointer to next atomic sequence which at most contains one variant. */
    pointer?: Metriport.fhir.Reference[];
    /** Information about chromosome structure variation. */
    structureVariant?: Metriport.fhir.MolecularSequenceStructureVariant[];
}
