/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * An association between a patient and an organization / healthcare provider(s) during which time encounters may occur. The managing organization assumes a level of responsibility for the patient during this time.
 */
export interface EpisodeOfCare extends Metriport.fhir.BaseResource {
    resourceType: "EpisodeOfCare";
    /** The EpisodeOfCare may be known by different identifiers for different contexts of use, such as when an external agency is tracking the Episode for funding purposes. */
    identifier?: Metriport.fhir.Identifier[];
    /** planned | waitlist | active | onhold | finished | cancelled. */
    status?: Metriport.fhir.EpisodeOfCareStatus;
    /** The history of statuses that the EpisodeOfCare has been through (without requiring processing the history of the resource). */
    statusHistory?: Metriport.fhir.EpisodeOfCareStatusHistory[];
    /** A classification of the type of episode of care; e.g. specialist referral, disease management, type of funded care. */
    type?: Metriport.fhir.CodeableConcept[];
    /** The list of diagnosis relevant to this episode of care. */
    diagnosis?: Metriport.fhir.EpisodeOfCareDiagnosis[];
    /** The patient who is the focus of this episode of care. */
    patient: Metriport.fhir.Reference;
    /** The organization that has assumed the specific responsibilities for the specified duration. */
    managingOrganization?: Metriport.fhir.Reference;
    /** The interval during which the managing organization assumes the defined responsibility. */
    period?: Metriport.fhir.Period;
    /** Referral Request(s) that are fulfilled by this EpisodeOfCare, incoming referrals. */
    referralRequest?: Metriport.fhir.Reference[];
    /** The practitioner that is the care manager/care coordinator for this patient. */
    careManager?: Metriport.fhir.Reference;
    /** The list of practitioners that may be facilitating this episode of care for specific purposes. */
    team?: Metriport.fhir.Reference[];
    /** The set of accounts that may be used for billing for this EpisodeOfCare. */
    account?: Metriport.fhir.Reference[];
}
