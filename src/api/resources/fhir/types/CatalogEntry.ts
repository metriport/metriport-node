/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * Catalog entries are wrappers that contextualize items included in a catalog.
 */
export interface CatalogEntry extends Metriport.fhir.BaseResource {
    resourceType: "CatalogEntry";
    /** Used in supporting different identifiers for the same product, e.g. manufacturer code and retailer code. */
    identifier?: Metriport.fhir.Identifier[];
    /** The type of item - medication, device, service, protocol or other. */
    type?: Metriport.fhir.CodeableConcept;
    /** Whether the entry represents an orderable item. */
    orderable?: boolean;
    /** The item in a catalog or definition. */
    referencedItem: Metriport.fhir.Reference;
    /** Used in supporting related concepts, e.g. NDC to RxNorm. */
    additionalIdentifier?: Metriport.fhir.Identifier[];
    /** Classes of devices, or ATC for medication. */
    classification?: Metriport.fhir.CodeableConcept[];
    /** Used to support catalog exchange even for unsupported products, e.g. getting list of medications even if not prescribable. */
    status?: Metriport.fhir.CatalogEntryStatus;
    /** The time period in which this catalog entry is expected to be active. */
    validityPeriod?: Metriport.fhir.Period;
    /** The date until which this catalog entry is expected to be active. */
    validTo?: Metriport.fhir.DateTime;
    /** Typically date of issue is different from the beginning of the validity. This can be used to see when an item was last updated. */
    lastUpdated?: Metriport.fhir.DateTime;
    /** Used for examplefor Out of Formulary, or any specifics. */
    additionalCharacteristic?: Metriport.fhir.CodeableConcept[];
    /** User for example for ATC classification, or. */
    additionalClassification?: Metriport.fhir.CodeableConcept[];
    /** Used for example, to point to a substance, or to a device used to administer a medication. */
    relatedEntry?: Metriport.fhir.CatalogEntryRelatedEntry[];
}
