/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * A photo, video, or audio recording acquired or used in healthcare. The actual content may be inline or provided by direct reference.
 */
export interface Media extends Metriport.fhir.BaseResource {
    resourceType: "Media";
    /** Identifiers associated with the image - these may include identifiers for the image itself, identifiers for the context of its collection (e.g. series ids) and context ids such as accession numbers or other workflow identifiers. */
    identifier?: Metriport.fhir.Identifier[];
    /** A procedure that is fulfilled in whole or in part by the creation of this media. */
    basedOn?: Metriport.fhir.Reference[];
    /** A larger event of which this particular event is a component or step. */
    partOf?: Metriport.fhir.Reference[];
    /** The current state of the {{title}}. */
    status?: Metriport.fhir.Code;
    /** A code that classifies whether the media is an image, video or audio recording or some other media category. */
    type?: Metriport.fhir.CodeableConcept;
    /** Details of the type of the media - usually, how it was acquired (what type of device). If images sourced from a DICOM system, are wrapped in a Media resource, then this is the modality. */
    modality?: Metriport.fhir.CodeableConcept;
    /** The name of the imaging view e.g. Lateral or Antero-posterior (AP). */
    view?: Metriport.fhir.CodeableConcept;
    /** Who/What this Media is a record of. */
    subject?: Metriport.fhir.Reference;
    /** The encounter that establishes the context for this media. */
    encounter?: Metriport.fhir.Reference;
    /** The date and time(s) at which the media was collected. */
    createdDateTime?: string;
    /** The date and time(s) at which the media was collected. */
    createdPeriod?: Metriport.fhir.Period;
    /** The date and time this version of the media was made available to providers, typically after having been reviewed. */
    issued?: Metriport.fhir.Instant;
    /** The person who administered the collection of the image. */
    operator?: Metriport.fhir.Reference;
    /** Describes why the event occurred in coded or textual form. */
    reasonCode?: Metriport.fhir.CodeableConcept[];
    /** Indicates the site on the subject's body where the observation was made (i.e. the target site). */
    bodySite?: Metriport.fhir.CodeableConcept;
    /** The name of the device / manufacturer of the device that was used to make the recording. */
    deviceName?: string;
    /** The device used to collect the media. */
    device?: Metriport.fhir.Reference;
    /** Height of the image in pixels (photo/video). */
    height?: Metriport.fhir.PositiveInt;
    /** Width of the image in pixels (photo/video). */
    width?: Metriport.fhir.PositiveInt;
    /** The number of frames in a photo. This is used with a multi-page fax, or an imaging acquisition context that takes multiple slices in a single image, or an animated gif. If there is more than one frame, this SHALL have a value in order to alert interface software that a multi-frame capable rendering widget is required. */
    frames?: Metriport.fhir.PositiveInt;
    /** The duration of the recording in seconds - for audio and video. */
    duration?: Metriport.fhir.Decimal;
    /** The actual content of the media - inline or by direct reference to the media source file. */
    content: Metriport.fhir.Attachment;
    /** Comments made about the media by the performer, subject or other participants. */
    note?: Metriport.fhir.Annotation[];
}
