/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * Invoice containing collected ChargeItems from an Account with calculated individual and total price for Billing purpose.
 */
export interface Invoice extends Metriport.fhir.BaseResource {
    resourceType: "Invoice";
    /** Identifier of this Invoice, often used for reference in correspondence about this invoice or for tracking of payments. */
    identifier?: Metriport.fhir.Identifier[];
    /** The current state of the Invoice. */
    status?: Metriport.fhir.InvoiceStatus;
    /** In case of Invoice cancellation a reason must be given (entered in error, superseded by corrected invoice etc.). */
    cancelledReason?: string;
    /** Type of Invoice depending on domain, realm an usage (e.g. internal/external, dental, preliminary). */
    type?: Metriport.fhir.CodeableConcept;
    /** The individual or set of individuals receiving the goods and services billed in this invoice. */
    subject?: Metriport.fhir.Reference;
    /** The individual or Organization responsible for balancing of this invoice. */
    recipient?: Metriport.fhir.Reference;
    /** Date/time(s) of when this Invoice was posted. */
    date?: Metriport.fhir.DateTime;
    /** Indicates who or what performed or participated in the charged service. */
    participant?: Metriport.fhir.InvoiceParticipant[];
    /** The organizationissuing the Invoice. */
    issuer?: Metriport.fhir.Reference;
    /** Account which is supposed to be balanced with this Invoice. */
    account?: Metriport.fhir.Reference;
    /** Each line item represents one charge for goods and services rendered. Details such as date, code and amount are found in the referenced ChargeItem resource. */
    lineItem?: Metriport.fhir.InvoiceLineItem[];
    /** The total amount for the Invoice may be calculated as the sum of the line items with surcharges/deductions that apply in certain conditions. The priceComponent element can be used to offer transparency to the recipient of the Invoice of how the total price was calculated. */
    totalPriceComponent?: Metriport.fhir.InvoicePriceComponent[];
    /** Invoice total , taxes excluded. */
    totalNet?: Metriport.fhir.Money;
    /** Invoice total, tax included. */
    totalGross?: Metriport.fhir.Money;
    /** Payment details such as banking details, period of payment, deductibles, methods of payment. */
    paymentTerms?: Metriport.fhir.Markdown;
    /** Comments made about the invoice by the issuer, subject, or other participants. */
    note?: Metriport.fhir.Annotation[];
}
