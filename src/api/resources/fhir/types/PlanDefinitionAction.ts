/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * This resource allows for the definition of various types of plans as a sharable, consumable, and executable artifact. The resource is general enough to support the description of a broad range of clinical artifacts such as clinical decision support rules, order sets and protocols.
 */
export interface PlanDefinitionAction {
    /** Unique id for the element within a resource (for internal references). This may be any string value that does not contain spaces. */
    id?: string;
    /** May be used to represent additional information that is not part of the basic definition of the element. To make the use of extensions safe and manageable, there is a strict set of governance applied to the definition and use of extensions. Though any implementer can define an extension, there is a set of requirements that SHALL be met as part of the definition of the extension. */
    extension?: Metriport.fhir.Extension[];
    /** May be used to represent additional information that is not part of the basic definition of the element and that modifies the understanding of the element in which it is contained and/or the understanding of the containing element's descendants. Usually modifier elements provide negation or qualification. To make the use of extensions safe and manageable, there is a strict set of governance applied to the definition and use of extensions. Though any implementer can define an extension, there is a set of requirements that SHALL be met as part of the definition of the extension. Applications processing a resource are required to check for modifier extensions. Modifier extensions SHALL NOT change the meaning of any elements on Resource or DomainResource (including cannot change the meaning of modifierExtension itself). */
    modifierExtension?: Metriport.fhir.Extension[];
    /** A user-visible prefix for the action. */
    prefix?: string;
    /** The title of the action displayed to a user. */
    title?: string;
    /** A brief description of the action used to provide a summary to display to the user. */
    description?: string;
    /** A text equivalent of the action to be performed. This provides a human-interpretable description of the action when the definition is consumed by a system that might not be capable of interpreting it dynamically. */
    textEquivalent?: string;
    /** Indicates how quickly the action should be addressed with respect to other actions. */
    priority?: Metriport.fhir.Code;
    /** A code that provides meaning for the action or action group. For example, a section may have a LOINC code for the section of a documentation template. */
    code?: Metriport.fhir.CodeableConcept[];
    /** A description of why this action is necessary or appropriate. */
    reason?: Metriport.fhir.CodeableConcept[];
    /** Didactic or other informational resources associated with the action that can be provided to the CDS recipient. Information resources can include inline text commentary and links to web resources. */
    documentation?: Metriport.fhir.RelatedArtifact[];
    /** Identifies goals that this action supports. The reference must be to a goal element defined within this plan definition. */
    goalId?: Metriport.fhir.Id[];
    /** A code or group definition that describes the intended subject of the action and its children, if any. */
    subjectCodeableConcept?: Metriport.fhir.CodeableConcept;
    /** A code or group definition that describes the intended subject of the action and its children, if any. */
    subjectReference?: Metriport.fhir.Reference;
    /** A description of when the action should be triggered. */
    trigger?: Metriport.fhir.TriggerDefinition[];
    /** An expression that describes applicability criteria or start/stop conditions for the action. */
    condition?: Metriport.fhir.PlanDefinitionCondition[];
    /** Defines input data requirements for the action. */
    input?: Metriport.fhir.DataRequirement[];
    /** Defines the outputs of the action, if any. */
    output?: Metriport.fhir.DataRequirement[];
    /** A relationship to another action such as "before" or "30-60 minutes after start of". */
    relatedAction?: Metriport.fhir.PlanDefinitionRelatedAction[];
    /** An optional value describing when the action should be performed. */
    timingDateTime?: string;
    /** An optional value describing when the action should be performed. */
    timingAge?: Metriport.fhir.Age;
    /** An optional value describing when the action should be performed. */
    timingPeriod?: Metriport.fhir.Period;
    /** An optional value describing when the action should be performed. */
    timingDuration?: Metriport.fhir.Duration;
    /** An optional value describing when the action should be performed. */
    timingRange?: Metriport.fhir.Range;
    /** An optional value describing when the action should be performed. */
    timingTiming?: Metriport.fhir.Timing;
    /** Indicates who should participate in performing the action described. */
    participant?: Metriport.fhir.PlanDefinitionParticipant[];
    /** The type of action to perform (create, update, remove). */
    type?: Metriport.fhir.CodeableConcept;
    /** Defines the grouping behavior for the action and its children. */
    groupingBehavior?: Metriport.fhir.PlanDefinitionActionGroupingBehavior;
    /** Defines the selection behavior for the action and its children. */
    selectionBehavior?: Metriport.fhir.PlanDefinitionActionSelectionBehavior;
    /** Defines the required behavior for the action. */
    requiredBehavior?: Metriport.fhir.PlanDefinitionActionRequiredBehavior;
    /** Defines whether the action should usually be preselected. */
    precheckBehavior?: Metriport.fhir.PlanDefinitionActionPrecheckBehavior;
    /** Defines whether the action can be selected multiple times. */
    cardinalityBehavior?: Metriport.fhir.PlanDefinitionActionCardinalityBehavior;
    /** A reference to an ActivityDefinition that describes the action to be taken in detail, or a PlanDefinition that describes a series of actions to be taken. */
    definitionCanonical?: string;
    /** A reference to an ActivityDefinition that describes the action to be taken in detail, or a PlanDefinition that describes a series of actions to be taken. */
    definitionUri?: string;
    /** A reference to a StructureMap resource that defines a transform that can be executed to produce the intent resource using the ActivityDefinition instance as the input. */
    transform?: Metriport.fhir.Canonical;
    /** Customizations that should be applied to the statically defined resource. For example, if the dosage of a medication must be computed based on the patient's weight, a customization would be used to specify an expression that calculated the weight, and the path on the resource that would contain the result. */
    dynamicValue?: Metriport.fhir.PlanDefinitionDynamicValue[];
    /** Sub actions that are contained within the action. The behavior of this action determines the functionality of the sub-actions. For example, a selection behavior of at-most-one indicates that of the sub-actions, at most one may be chosen as part of realizing the action definition. */
    action?: Metriport.fhir.PlanDefinitionAction[];
}
