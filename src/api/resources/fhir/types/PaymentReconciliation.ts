/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * This resource provides the details including amount of a payment and allocates the payment items being paid.
 */
export interface PaymentReconciliation extends Metriport.fhir.BaseResource {
    resourceType: "PaymentReconciliation";
    /** A unique identifier assigned to this payment reconciliation. */
    identifier?: Metriport.fhir.Identifier[];
    /** The status of the resource instance. */
    status?: Metriport.fhir.Code;
    /** The period of time for which payments have been gathered into this bulk payment for settlement. */
    period?: Metriport.fhir.Period;
    /** The date when the resource was created. */
    created?: Metriport.fhir.DateTime;
    /** The party who generated the payment. */
    paymentIssuer?: Metriport.fhir.Reference;
    /** Original request resource reference. */
    request?: Metriport.fhir.Reference;
    /** The practitioner who is responsible for the services rendered to the patient. */
    requestor?: Metriport.fhir.Reference;
    /** The outcome of a request for a reconciliation. */
    outcome?: Metriport.fhir.PaymentReconciliationOutcome;
    /** A human readable description of the status of the request for the reconciliation. */
    disposition?: string;
    /** The date of payment as indicated on the financial instrument. */
    paymentDate?: string;
    /** Total payment amount as indicated on the financial instrument. */
    paymentAmount: Metriport.fhir.Money;
    /** Issuer's unique identifier for the payment instrument. */
    paymentIdentifier?: Metriport.fhir.Identifier;
    /** Distribution of the payment amount for a previously acknowledged payable. */
    detail?: Metriport.fhir.PaymentReconciliationDetail[];
    /** A code for the form to be used for printing the content. */
    formCode?: Metriport.fhir.CodeableConcept;
    /** A note that describes or explains the processing in a human readable form. */
    processNote?: Metriport.fhir.PaymentReconciliationProcessNote[];
}
