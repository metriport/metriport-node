/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * Demographics and other administrative information about an individual or animal receiving care or other health-related services.
 */
export interface Patient extends Metriport.fhir.BaseResource {
    resourceType: "Patient";
    /** An identifier for this patient. */
    identifier?: Metriport.fhir.Identifier[];
    /** Whether this patient record is in active use. Many systems use this property to mark as non-current patients, such as those that have not been seen for a period of time based on an organization's business rules. It is often used to filter patient lists to exclude inactive patients Deceased patients may also be marked as inactive for the same reasons, but may be active for some time after death. */
    active?: boolean;
    /** A name associated with the individual. */
    name?: Metriport.fhir.HumanName[];
    /** A contact detail (e.g. a telephone number or an email address) by which the individual may be contacted. */
    telecom?: Metriport.fhir.ContactPoint[];
    /** Administrative Gender - the gender that the patient is considered to have for administration and record keeping purposes. */
    gender?: Metriport.fhir.PatientGender;
    /** The date of birth for the individual. */
    birthDate?: string;
    /** Indicates if the individual is deceased or not. */
    deceasedBoolean?: boolean;
    /** Indicates if the individual is deceased or not. */
    deceasedDateTime?: string;
    /** An address for the individual. */
    address?: Metriport.fhir.Address[];
    /** This field contains a patient's most recent marital (civil) status. */
    maritalStatus?: Metriport.fhir.CodeableConcept;
    /** Indicates whether the patient is part of a multiple (boolean) or indicates the actual birth order (integer). */
    multipleBirthBoolean?: boolean;
    /** Indicates whether the patient is part of a multiple (boolean) or indicates the actual birth order (integer). */
    multipleBirthInteger?: number;
    /** Image of the patient. */
    photo?: Metriport.fhir.Attachment[];
    /** A contact party (e.g. guardian, partner, friend) for the patient. */
    contact?: Metriport.fhir.PatientContact[];
    /** A language which may be used to communicate with the patient about his or her health. */
    communication?: Metriport.fhir.PatientCommunication[];
    /** Patient's nominated care provider. */
    generalPractitioner?: Metriport.fhir.Reference[];
    /** Organization that is the custodian of the patient record. */
    managingOrganization?: Metriport.fhir.Reference;
    /** Link to another patient resource that concerns the same actual patient. */
    link?: Metriport.fhir.PatientLink[];
}
