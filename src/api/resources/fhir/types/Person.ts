/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * Demographics and administrative information about a person independent of a specific health-related context.
 */
export interface Person extends Metriport.fhir.BaseResource {
    resourceType: "Person";
    /** Identifier for a person within a particular scope. */
    identifier?: Metriport.fhir.Identifier[];
    /** A name associated with the person. */
    name?: Metriport.fhir.HumanName[];
    /** A contact detail for the person, e.g. a telephone number or an email address. */
    telecom?: Metriport.fhir.ContactPoint[];
    /** Administrative Gender. */
    gender?: Metriport.fhir.PersonGender;
    /** The birth date for the person. */
    birthDate?: string;
    /** One or more addresses for the person. */
    address?: Metriport.fhir.Address[];
    /** An image that can be displayed as a thumbnail of the person to enhance the identification of the individual. */
    photo?: Metriport.fhir.Attachment;
    /** The organization that is the custodian of the person record. */
    managingOrganization?: Metriport.fhir.Reference;
    /** Whether this person's record is in active use. */
    active?: boolean;
    /** Link to a resource that concerns the same actual person. */
    link?: Metriport.fhir.PersonLink[];
}
