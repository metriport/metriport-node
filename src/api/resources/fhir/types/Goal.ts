/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * Describes the intended objective(s) for a patient, group or organization care, for example, weight loss, restoring an activity of daily living, obtaining herd immunity via immunization, meeting a process improvement objective, etc.
 */
export interface Goal extends Metriport.fhir.BaseResource {
    resourceType: "Goal";
    /** Business identifiers assigned to this goal by the performer or other systems which remain constant as the resource is updated and propagates from server to server. */
    identifier?: Metriport.fhir.Identifier[];
    /** The state of the goal throughout its lifecycle. */
    lifecycleStatus?: Metriport.fhir.GoalLifecycleStatus;
    /** Describes the progression, or lack thereof, towards the goal against the target. */
    achievementStatus?: Metriport.fhir.CodeableConcept;
    /** Indicates a category the goal falls within. */
    category?: Metriport.fhir.CodeableConcept[];
    /** Identifies the mutually agreed level of importance associated with reaching/sustaining the goal. */
    priority?: Metriport.fhir.CodeableConcept;
    /** Human-readable and/or coded description of a specific desired objective of care, such as "control blood pressure" or "negotiate an obstacle course" or "dance with child at wedding". */
    description: Metriport.fhir.CodeableConcept;
    /** Identifies the patient, group or organization for whom the goal is being established. */
    subject: Metriport.fhir.Reference;
    /** The date or event after which the goal should begin being pursued. */
    startDate?: string;
    /** The date or event after which the goal should begin being pursued. */
    startCodeableConcept?: Metriport.fhir.CodeableConcept;
    /** Indicates what should be done by when. */
    target?: Metriport.fhir.GoalTarget[];
    /** Identifies when the current status. I.e. When initially created, when achieved, when cancelled, etc. */
    statusDate?: string;
    /** Captures the reason for the current status. */
    statusReason?: string;
    /** Indicates whose goal this is - patient goal, practitioner goal, etc. */
    expressedBy?: Metriport.fhir.Reference;
    /** The identified conditions and other health record elements that are intended to be addressed by the goal. */
    addresses?: Metriport.fhir.Reference[];
    /** Any comments related to the goal. */
    note?: Metriport.fhir.Annotation[];
    /** Identifies the change (or lack of change) at the point when the status of the goal is assessed. */
    outcomeCode?: Metriport.fhir.CodeableConcept[];
    /** Details of what's changed (or not changed). */
    outcomeReference?: Metriport.fhir.Reference[];
}
