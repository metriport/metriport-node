/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * Describes a comparison of an immunization event against published recommendations to determine if the administration is "valid" in relation to those recommendations.
 */
export interface ImmunizationEvaluation extends Metriport.fhir.BaseResource {
    resourceType: "ImmunizationEvaluation";
    /** A unique identifier assigned to this immunization evaluation record. */
    identifier?: Metriport.fhir.Identifier[];
    /** Indicates the current status of the evaluation of the vaccination administration event. */
    status?: Metriport.fhir.Code;
    /** The individual for whom the evaluation is being done. */
    patient: Metriport.fhir.Reference;
    /** The date the evaluation of the vaccine administration event was performed. */
    date?: Metriport.fhir.DateTime;
    /** Indicates the authority who published the protocol (e.g. ACIP). */
    authority?: Metriport.fhir.Reference;
    /** The vaccine preventable disease the dose is being evaluated against. */
    targetDisease: Metriport.fhir.CodeableConcept;
    /** The vaccine administration event being evaluated. */
    immunizationEvent: Metriport.fhir.Reference;
    /** Indicates if the dose is valid or not valid with respect to the published recommendations. */
    doseStatus: Metriport.fhir.CodeableConcept;
    /** Provides an explanation as to why the vaccine administration event is valid or not relative to the published recommendations. */
    doseStatusReason?: Metriport.fhir.CodeableConcept[];
    /** Additional information about the evaluation. */
    description?: string;
    /** One possible path to achieve presumed immunity against a disease - within the context of an authority. */
    series?: string;
    /** Nominal position in a series. */
    doseNumberPositiveInt?: number;
    /** Nominal position in a series. */
    doseNumberString?: string;
    /** The recommended number of doses to achieve immunity. */
    seriesDosesPositiveInt?: number;
    /** The recommended number of doses to achieve immunity. */
    seriesDosesString?: string;
}
