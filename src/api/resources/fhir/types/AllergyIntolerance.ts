/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * Risk of harmful or undesirable, physiological response which is unique to an individual and associated with exposure to a substance.
 */
export interface AllergyIntolerance extends Metriport.fhir.BaseResource {
    resourceType: "AllergyIntolerance";
    /** Business identifiers assigned to this AllergyIntolerance by the performer or other systems which remain constant as the resource is updated and propagates from server to server. */
    identifier?: Metriport.fhir.Identifier[];
    /** The clinical status of the allergy or intolerance. */
    clinicalStatus?: Metriport.fhir.CodeableConcept;
    /** Assertion about certainty associated with the propensity, or potential risk, of a reaction to the identified substance (including pharmaceutical product). */
    verificationStatus?: Metriport.fhir.CodeableConcept;
    /** Identification of the underlying physiological mechanism for the reaction risk. */
    type?: Metriport.fhir.AllergyIntoleranceType;
    /** Category of the identified substance. */
    category?: Metriport.fhir.AllergyIntoleranceCategoryItem[];
    /** Estimate of the potential clinical harm, or seriousness, of the reaction to the identified substance. */
    criticality?: Metriport.fhir.AllergyIntoleranceCriticality;
    /** Code for an allergy or intolerance statement (either a positive or a negated/excluded statement). This may be a code for a substance or pharmaceutical product that is considered to be responsible for the adverse reaction risk (e.g., "Latex"), an allergy or intolerance condition (e.g., "Latex allergy"), or a negated/excluded code for a specific substance or class (e.g., "No latex allergy") or a general or categorical negated statement (e.g., "No known allergy", "No known drug allergies"). Note: the substance for a specific reaction may be different from the substance identified as the cause of the risk, but it must be consistent with it. For instance, it may be a more specific substance (e.g. a brand medication) or a composite product that includes the identified substance. It must be clinically safe to only process the 'code' and ignore the 'reaction.substance'. If a receiving system is unable to confirm that AllergyIntolerance.reaction.substance falls within the semantic scope of AllergyIntolerance.code, then the receiving system should ignore AllergyIntolerance.reaction.substance. */
    code?: Metriport.fhir.CodeableConcept;
    /** The patient who has the allergy or intolerance. */
    patient: Metriport.fhir.Reference;
    /** The encounter when the allergy or intolerance was asserted. */
    encounter?: Metriport.fhir.Reference;
    /** Estimated or actual date, date-time, or age when allergy or intolerance was identified. */
    onsetDateTime?: string;
    /** Estimated or actual date, date-time, or age when allergy or intolerance was identified. */
    onsetAge?: Metriport.fhir.Age;
    /** Estimated or actual date, date-time, or age when allergy or intolerance was identified. */
    onsetPeriod?: Metriport.fhir.Period;
    /** Estimated or actual date, date-time, or age when allergy or intolerance was identified. */
    onsetRange?: Metriport.fhir.Range;
    /** Estimated or actual date, date-time, or age when allergy or intolerance was identified. */
    onsetString?: string;
    /** The recordedDate represents when this particular AllergyIntolerance record was created in the system, which is often a system-generated date. */
    recordedDate?: Metriport.fhir.DateTime;
    /** Individual who recorded the record and takes responsibility for its content. */
    recorder?: Metriport.fhir.Reference;
    /** The source of the information about the allergy that is recorded. */
    asserter?: Metriport.fhir.Reference;
    /** Represents the date and/or time of the last known occurrence of a reaction event. */
    lastOccurrence?: Metriport.fhir.DateTime;
    /** Additional narrative about the propensity for the Adverse Reaction, not captured in other fields. */
    note?: Metriport.fhir.Annotation[];
    /** Details about each adverse reaction event linked to exposure to the identified substance. */
    reaction?: Metriport.fhir.AllergyIntoleranceReaction[];
}
