/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * A reply to an appointment request for a patient and/or practitioner(s), such as a confirmation or rejection.
 */
export interface AppointmentResponse extends Metriport.fhir.BaseResource {
    resourceType: "AppointmentResponse";
    /** This records identifiers associated with this appointment response concern that are defined by business processes and/ or used to refer to it when a direct URL reference to the resource itself is not appropriate. */
    identifier?: Metriport.fhir.Identifier[];
    /** Appointment that this response is replying to. */
    appointment: Metriport.fhir.Reference;
    /** Date/Time that the appointment is to take place, or requested new start time. */
    start?: Metriport.fhir.Instant;
    /** This may be either the same as the appointment request to confirm the details of the appointment, or alternately a new time to request a re-negotiation of the end time. */
    end?: Metriport.fhir.Instant;
    /** Role of participant in the appointment. */
    participantType?: Metriport.fhir.CodeableConcept[];
    /** A Person, Location, HealthcareService, or Device that is participating in the appointment. */
    actor?: Metriport.fhir.Reference;
    /** Participation status of the participant. When the status is declined or tentative if the start/end times are different to the appointment, then these times should be interpreted as a requested time change. When the status is accepted, the times can either be the time of the appointment (as a confirmation of the time) or can be empty. */
    participantStatus?: Metriport.fhir.Code;
    /** Additional comments about the appointment. */
    comment?: string;
}
