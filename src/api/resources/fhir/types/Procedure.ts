/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * An action that is or was performed on or for a patient. This can be a physical intervention like an operation, or less invasive like long term services, counseling, or hypnotherapy.
 */
export interface Procedure extends Metriport.fhir.BaseResource {
    resourceType: "Procedure";
    /** Business identifiers assigned to this procedure by the performer or other systems which remain constant as the resource is updated and is propagated from server to server. */
    identifier?: Metriport.fhir.Identifier[];
    /** The URL pointing to a FHIR-defined protocol, guideline, order set or other definition that is adhered to in whole or in part by this Procedure. */
    instantiatesCanonical?: Metriport.fhir.Canonical[];
    /** The URL pointing to an externally maintained protocol, guideline, order set or other definition that is adhered to in whole or in part by this Procedure. */
    instantiatesUri?: Metriport.fhir.Uri[];
    /** A reference to a resource that contains details of the request for this procedure. */
    basedOn?: Metriport.fhir.Reference[];
    /** A larger event of which this particular procedure is a component or step. */
    partOf?: Metriport.fhir.Reference[];
    /** A code specifying the state of the procedure. Generally, this will be the in-progress or completed state. */
    status?: Metriport.fhir.Code;
    /** Captures the reason for the current state of the procedure. */
    statusReason?: Metriport.fhir.CodeableConcept;
    /** A code that classifies the procedure for searching, sorting and display purposes (e.g. "Surgical Procedure"). */
    category?: Metriport.fhir.CodeableConcept;
    /** The specific procedure that is performed. Use text if the exact nature of the procedure cannot be coded (e.g. "Laparoscopic Appendectomy"). */
    code?: Metriport.fhir.CodeableConcept;
    /** The person, animal or group on which the procedure was performed. */
    subject: Metriport.fhir.Reference;
    /** The Encounter during which this Procedure was created or performed or to which the creation of this record is tightly associated. */
    encounter?: Metriport.fhir.Reference;
    /** Estimated or actual date, date-time, period, or age when the procedure was performed. Allows a period to support complex procedures that span more than one date, and also allows for the length of the procedure to be captured. */
    performedDateTime?: string;
    /** Estimated or actual date, date-time, period, or age when the procedure was performed. Allows a period to support complex procedures that span more than one date, and also allows for the length of the procedure to be captured. */
    performedPeriod?: Metriport.fhir.Period;
    /** Estimated or actual date, date-time, period, or age when the procedure was performed. Allows a period to support complex procedures that span more than one date, and also allows for the length of the procedure to be captured. */
    performedString?: string;
    /** Estimated or actual date, date-time, period, or age when the procedure was performed. Allows a period to support complex procedures that span more than one date, and also allows for the length of the procedure to be captured. */
    performedAge?: Metriport.fhir.Age;
    /** Estimated or actual date, date-time, period, or age when the procedure was performed. Allows a period to support complex procedures that span more than one date, and also allows for the length of the procedure to be captured. */
    performedRange?: Metriport.fhir.Range;
    /** Individual who recorded the record and takes responsibility for its content. */
    recorder?: Metriport.fhir.Reference;
    /** Individual who is making the procedure statement. */
    asserter?: Metriport.fhir.Reference;
    /** Limited to "real" people rather than equipment. */
    performer?: Metriport.fhir.ProcedurePerformer[];
    /** The location where the procedure actually happened. E.g. a newborn at home, a tracheostomy at a restaurant. */
    location?: Metriport.fhir.Reference;
    /** The coded reason why the procedure was performed. This may be a coded entity of some type, or may simply be present as text. */
    reasonCode?: Metriport.fhir.CodeableConcept[];
    /** The justification of why the procedure was performed. */
    reasonReference?: Metriport.fhir.Reference[];
    /** Detailed and structured anatomical location information. Multiple locations are allowed - e.g. multiple punch biopsies of a lesion. */
    bodySite?: Metriport.fhir.CodeableConcept[];
    /** The outcome of the procedure - did it resolve the reasons for the procedure being performed? */
    outcome?: Metriport.fhir.CodeableConcept;
    /** This could be a histology result, pathology report, surgical report, etc. */
    report?: Metriport.fhir.Reference[];
    /** Any complications that occurred during the procedure, or in the immediate post-performance period. These are generally tracked separately from the notes, which will typically describe the procedure itself rather than any 'post procedure' issues. */
    complication?: Metriport.fhir.CodeableConcept[];
    /** Any complications that occurred during the procedure, or in the immediate post-performance period. */
    complicationDetail?: Metriport.fhir.Reference[];
    /** If the procedure required specific follow up - e.g. removal of sutures. The follow up may be represented as a simple note or could potentially be more complex, in which case the CarePlan resource can be used. */
    followUp?: Metriport.fhir.CodeableConcept[];
    /** Any other notes and comments about the procedure. */
    note?: Metriport.fhir.Annotation[];
    /** A device that is implanted, removed or otherwise manipulated (calibration, battery replacement, fitting a prosthesis, attaching a wound-vac, etc.) as a focal portion of the Procedure. */
    focalDevice?: Metriport.fhir.ProcedureFocalDevice[];
    /** Identifies medications, devices and any other substance used as part of the procedure. */
    usedReference?: Metriport.fhir.Reference[];
    /** Identifies coded items that were used as part of the procedure. */
    usedCode?: Metriport.fhir.CodeableConcept[];
}
