/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Metriport from "../../..";

/**
 * Actual or potential/avoided event causing unintended physical injury resulting from or contributed to by medical care, a research study or other healthcare setting factors that requires additional monitoring, treatment, or hospitalization, or that results in death.
 */
export interface AdverseEvent extends Metriport.fhir.BaseResource {
    resourceType: "AdverseEvent";
    /** Business identifiers assigned to this adverse event by the performer or other systems which remain constant as the resource is updated and propagates from server to server. */
    identifier?: Metriport.fhir.Identifier;
    /** Whether the event actually happened, or just had the potential to. Note that this is independent of whether anyone was affected or harmed or how severely. */
    actuality?: Metriport.fhir.AdverseEventActuality;
    /** The overall type of event, intended for search and filtering purposes. */
    category?: Metriport.fhir.CodeableConcept[];
    /** This element defines the specific type of event that occurred or that was prevented from occurring. */
    event?: Metriport.fhir.CodeableConcept;
    /** This subject or group impacted by the event. */
    subject: Metriport.fhir.Reference;
    /** The Encounter during which AdverseEvent was created or to which the creation of this record is tightly associated. */
    encounter?: Metriport.fhir.Reference;
    /** The date (and perhaps time) when the adverse event occurred. */
    date?: Metriport.fhir.DateTime;
    /** Estimated or actual date the AdverseEvent began, in the opinion of the reporter. */
    detected?: Metriport.fhir.DateTime;
    /** The date on which the existence of the AdverseEvent was first recorded. */
    recordedDate?: Metriport.fhir.DateTime;
    /** Includes information about the reaction that occurred as a result of exposure to a substance (for example, a drug or a chemical). */
    resultingCondition?: Metriport.fhir.Reference[];
    /** The information about where the adverse event occurred. */
    location?: Metriport.fhir.Reference;
    /** Assessment whether this event was of real importance. */
    seriousness?: Metriport.fhir.CodeableConcept;
    /** Describes the severity of the adverse event, in relation to the subject. Contrast to AdverseEvent.seriousness - a severe rash might not be serious, but a mild heart problem is. */
    severity?: Metriport.fhir.CodeableConcept;
    /** Describes the type of outcome from the adverse event. */
    outcome?: Metriport.fhir.CodeableConcept;
    /** Information on who recorded the adverse event. May be the patient or a practitioner. */
    recorder?: Metriport.fhir.Reference;
    /** Parties that may or should contribute or have contributed information to the adverse event, which can consist of one or more activities. Such information includes information leading to the decision to perform the activity and how to perform the activity (e.g. consultant), information that the activity itself seeks to reveal (e.g. informant of clinical history), or information about what activity was performed (e.g. informant witness). */
    contributor?: Metriport.fhir.Reference[];
    /** Describes the entity that is suspected to have caused the adverse event. */
    suspectEntity?: Metriport.fhir.AdverseEventSuspectEntity[];
    /** AdverseEvent.subjectMedicalHistory. */
    subjectMedicalHistory?: Metriport.fhir.Reference[];
    /** AdverseEvent.referenceDocument. */
    referenceDocument?: Metriport.fhir.Reference[];
    /** AdverseEvent.study. */
    study?: Metriport.fhir.Reference[];
}
